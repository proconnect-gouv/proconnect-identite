schemaVersion: "2.0.0"

commandTests:
  - name: "Try to connect to unresolved postgres"
    command: "npm"
    args:
      - start
    exitCode: 1
    expectedOutput: ["tsx --import ./src/instrument.ts src/index.ts"]
    expectedError: ["application is listening on port 3000"]

containerRunOptions:
  envFile: .env.test
